CREATE TABLE users (
   id INT NOT NULL,
   userName VARCHAR(50) NOT NULL,
   PRIMARY KEY (id)
);

--INSERT INTO users (id, userName) VALUES (101, 'usertestname');
--INSERT INTO users (id, userName)  VALUES (002, 'Nahara');
